const decodeTheRing = function (s, p) {

    // write your code here
    const n = s.length;
    const m = p.length;
    const dp = new Array(n + 1).fill().map(() => new Array(n + 1))

  };
  
  module.exports = decodeTheRing;